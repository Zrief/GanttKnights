{
  "render": {
    "description": "出图",
    "type": "object",
    "hint": "影响画面的参数。数值范围由后端强制夹取；这里的 minimum/maximum 只是文档，表单不读它们。改动任何一项，下一次出图就按新值渲染。",
    "items": {
      "title": {
        "description": "图片主标题",
        "type": "string",
        "default": "近期活动一览"
      },
      "left_offset_days": {
        "description": "时间窗左侧回溯天数",
        "type": "int",
        "default": 3,
        "minimum": 0,
        "maximum": 30,
        "hint": "图上显示今天之前多少天的活动（0 表示只看今天起）。"
      },
      "right_offset_days": {
        "description": "时间窗右侧前瞻天数",
        "type": "int",
        "default": 22,
        "minimum": 7,
        "maximum": 30,
        "hint": "图上显示今天之后多少天；实际右边界会对齐到当周周日。上限 30：再长时一条活动的色条会被压得很窄，名字与日期刻度的摆放判定开始贴边（实测 35 天余量已到 0~4px）。"
      },
      "background_file": {
        "description": "指定背景图文件",
        "type": "string",
        "default": "",
        "hint": "背景图目录内的文件名（如 沉沦者.WEBP），也可写绝对路径。留空则每天按日期自动取一张（同一天固定是同一张，换天才换）——同一天里群里看到的图长得一样，不会因为谁先谁后而换配色。"
      },
      "background_dir": {
        "description": "背景图目录",
        "type": "string",
        "default": "",
        "hint": "留空 = 插件自带的 背景图/ 目录（两张示例图）。换成自己的图建议放到 data/plugin_data/astrbot_plugin_ganttknights/ 下再指过来——插件升级不会覆盖那里。"
      }
    }
  },
  "panels": {
    "description": "底栏面板",
    "type": "object",
    "hint": "控制底栏三个上新区是否画进图里。",
    "items": {
      "voucher": {
        "description": "展示「凭证兑换」面板",
        "type": "bool",
        "default": true
      },
      "outfit": {
        "description": "展示「新增时装」面板",
        "type": "bool",
        "default": true
      },
      "module": {
        "description": "展示「新增模组」面板",
        "type": "bool",
        "default": true
      }
    }
  },
  "data": {
    "description": "数据源",
    "type": "object",
    "hint": "数据来自 PRTS Wiki，需要网络。请求条数与「进行中」判定的小时宽限是内部旋钮，不在这里暴露（内核默认 50 条 / 4 小时，可用 GK_API_LIMIT / GK_FUTURE_BUFFER_HOURS 覆盖）。",
    "items": {
      "auto_refresh_daily": {
        "description": "每天首次出图时自动更新数据",
        "type": "bool",
        "default": true,
        "hint": "关闭后只出图不爬取，数据靠自己跑 python cli.py --force 更新。"
      }
    }
  },
  "notify": {
    "description": "提醒节点",
    "type": "object",
    "hint": "这一节决定「什么算今天有事」：活动的开始/结束离今天正好是你列的那几天，就算命中。命中才说话——不每天播报清单，也不报数据日差。",
    "items": {
      "enabled": {
        "description": "节点提醒总开关",
        "type": "bool",
        "default": true,
        "hint": "关掉后：一个字的节点提醒都不发，每日推送只发图。⚠️ 它和最下面「只在有节点提醒时推送」是一对：总开关关着就永远没有提醒，那个选项再打开就等于永远不推送。"
      },
      "end_enabled": {
        "description": "提醒「活动即将结束」",
        "type": "bool",
        "default": true,
        "hint": "配合下面的「结束前提醒」天数使用。"
      },
      "start_enabled": {
        "description": "提醒「活动开启」",
        "type": "bool",
        "default": true,
        "hint": "配合下面的「开启前提醒」天数使用。"
      },
      "end_offsets": {
        "description": "结束前提醒（距结束的天数）",
        "type": "list",
        "default": [-3, -1],
        "hint": "命中就提醒：-3 = 结束前 3 天，-1 = 最后一天，0 = 结束当天。可写多个；留空 = 这类不提醒。",
        "condition": {
          "enabled": true,
          "end_enabled": true
        }
      },
      "start_offsets": {
        "description": "开启前提醒（距开始的天数）",
        "type": "list",
        "default": [0],
        "hint": "0 = 开启当天，-1 = 开启前一天。可写多个；留空 = 这类不提醒。",
        "condition": {
          "enabled": true,
          "start_enabled": true
        }
      },
      "require_reminder": {
        "description": "只在有节点提醒时推送",
        "type": "bool",
        "default": false,
        "hint": "开启后：当天没有任何节点命中时，那天的每日推送整次静默——连图都不发，群里一条消息都没有（状态页会写下原因）。平时每天都想收到图就别开。",
        "condition": {
          "enabled": true
        }
      }
    }
  },
  "push": {
    "description": "每日推送",
    "type": "object",
    "hint": "这一节只管「怎么发」：开不开、几点发、发给谁、图下面附什么话。「什么算今天有事」在上一节「提醒节点」。",
    "items": {
      "enabled": {
        "description": "开启每日推送",
        "type": "bool",
        "default": false,
        "hint": "开启前要有推送目标（见下面「推送目标会话列表」）。到点画一张图，发给列表里每个会话；那天有节点提醒的话，图下面再附一句话（见「推送文案模板」）。"
      },
      "time": {
        "description": "每日推送时刻（HH:MM，24 小时制）",
        "type": "string",
        "default": "08:00",
        "hint": "按运行 AstrBot 那台机器的本地时间。这里只列了几个常用时刻；想要别的时间，直接在配置文件里把它改成 HH:MM 也能生效（下拉只是图方便）。格式不对或越界会退回 08:00。",
        "options": ["06:00", "08:00", "09:00", "12:00", "18:00", "21:00"],
        "condition": {
          "enabled": true
        }
      },
      "targets": {
        "description": "推送目标会话列表",
        "type": "list",
        "default": [],
        "hint": "在要推送的会话里发 /订阅甘特图 即可加入（群聊、私聊都可以，可多个会话各订各的），/退订甘特图 移出；也能在这里直接增删——**想停掉某个会话的推送，把它从这一项里删掉即可**。会话标识可在该会话里发 /甘特图状态 查看。"
      },
      "text_template": {
        "description": "推送文案模板",
        "type": "string",
        "default": "{提醒}",
        "hint": "有节点提醒那天附在图下面的那句话。可用 {提醒}（提醒原文）、{日期}（今天，如 09-23）、{条目数}（图里的活动条数）；留空 = 不附文字，只发图。写别的花括号会原样显示，不会报错。",
        "condition": {
          "enabled": true
        }
      }
    }
  }
}
