{
  "render": {
    "description": "出图",
    "type": "object",
    "hint": "影响画面的参数。数值范围由后端强制夹取；这里的 minimum/maximum 只是文档，表单不读它们。",
    "items": {
      "title": {
        "description": "图片主标题",
        "type": "string",
        "default": "近期活动一览"
      },
      "left_offset_days": {
        "description": "时间窗左侧回溯天数",
        "type": "int",
        "default": 3,
        "minimum": 0,
        "maximum": 30,
        "hint": "图上显示今天之前多少天的活动（0 表示只看今天起）。"
      },
      "right_offset_days": {
        "description": "时间窗右侧前瞻天数",
        "type": "int",
        "default": 22,
        "minimum": 7,
        "maximum": 30,
        "hint": "图上显示今天之后多少天；实际右边界会对齐到当周周日。上限 30：再长时一条活动的色条会被压得很窄，名字与日期刻度的摆放判定开始贴边（实测 35 天余量已到 0~4px）。"
      },
      "remind_days": {
        "description": "过期提醒天数",
        "type": "int",
        "default": 3,
        "minimum": 1,
        "maximum": 30,
        "hint": "提醒文案里提前多少天提醒即将结束的活动。"
      },
      "reuse_seconds": {
        "description": "出图复用窗口（秒）",
        "type": "int",
        "default": 60,
        "minimum": 0,
        "maximum": 3600,
        "hint": "窗口内重复发指令直接复用上一张图，不重新渲染；0 表示每次都重画。"
      },
      "random_background": {
        "description": "随机背景图",
        "type": "bool",
        "default": true,
        "hint": "开启：每次随机取一张；关闭且未指定文件：固定用背景图目录里的第一张（出图可复现）。"
      },
      "background_file": {
        "description": "指定背景图文件",
        "type": "string",
        "default": "",
        "hint": "背景图目录内的文件名（如 沉沦者.WEBP），也可写绝对路径；填了就忽略上一项。"
      },
      "background_dir": {
        "description": "背景图目录",
        "type": "string",
        "default": "",
        "hint": "留空 = 插件自带的 背景图/ 目录（两张示例图）。换成自己的图建议放到 data/plugin_data/astrbot_plugin_ganttknights/ 下再指过来——插件升级不会覆盖那里。"
      }
    }
  },
  "panels": {
    "description": "底栏面板",
    "type": "object",
    "hint": "控制底栏三个上新区是否出现在图里。关掉的区不会画，提醒文案里也不会再提它。",
    "items": {
      "voucher": {
        "description": "展示「凭证兑换」面板",
        "type": "bool",
        "default": true
      },
      "outfit": {
        "description": "展示「新增时装」面板",
        "type": "bool",
        "default": true
      },
      "module": {
        "description": "展示「新增模组」面板",
        "type": "bool",
        "default": true
      }
    }
  },
  "data": {
    "description": "数据源",
    "type": "object",
    "hint": "数据来自 PRTS Wiki，需要网络。请求条数与「进行中」判定的小时宽限是内部旋钮，不在这里暴露（内核默认 50 条 / 4 小时，可用 GK_API_LIMIT / GK_FUTURE_BUFFER_HOURS 覆盖）。",
    "items": {
      "auto_refresh_daily": {
        "description": "每天首次出图时自动更新数据",
        "type": "bool",
        "default": true,
        "hint": "关闭后只出图不爬取，数据靠自己跑 python cli.py --force 更新。"
      }
    }
  }
}
