{
  "render": {
    "description": "出图",
    "type": "object",
    "hint": "影响画面的参数。数值范围由后端强制夹取；这里的 minimum/maximum 只是文档，表单不读它们。改动任何一项，下一次出图就按新值渲染。",
    "items": {
      "title": {
        "description": "图片主标题",
        "type": "string",
        "default": "近期活动一览"
      },
      "left_offset_days": {
        "description": "时间窗左侧回溯天数",
        "type": "int",
        "default": 3,
        "minimum": 0,
        "maximum": 30,
        "hint": "图上显示今天之前多少天的活动（0 表示只看今天起）。"
      },
      "right_offset_days": {
        "description": "时间窗右侧前瞻天数",
        "type": "int",
        "default": 22,
        "minimum": 7,
        "maximum": 30,
        "hint": "图上显示今天之后多少天；实际右边界会对齐到当周周日。上限 30：再长时一条活动的色条会被压得很窄，名字与日期刻度的摆放判定开始贴边（实测 35 天余量已到 0~4px）。"
      },
      "remind_days": {
        "description": "过期提醒天数",
        "type": "int",
        "default": 3,
        "minimum": 1,
        "maximum": 30,
        "hint": "提醒文案里提前多少天提醒即将结束的活动。"
      },
      "background_file": {
        "description": "指定背景图文件",
        "type": "string",
        "default": "",
        "hint": "背景图目录内的文件名（如 沉沦者.WEBP），也可写绝对路径。留空则每天按日期自动取一张（同一天固定是同一张，换天才换）——同一天里群里看到的图长得一样，不会因为谁先谁后而换配色。"
      },
      "background_dir": {
        "description": "背景图目录",
        "type": "string",
        "default": "",
        "hint": "留空 = 插件自带的 背景图/ 目录（两张示例图）。换成自己的图建议放到 data/plugin_data/astrbot_plugin_ganttknights/ 下再指过来——插件升级不会覆盖那里。"
      }
    }
  },
  "panels": {
    "description": "底栏面板",
    "type": "object",
    "hint": "控制底栏三个上新区是否出现在图里。关掉的区不会画，提醒文案里也不会再提它。",
    "items": {
      "voucher": {
        "description": "展示「凭证兑换」面板",
        "type": "bool",
        "default": true
      },
      "outfit": {
        "description": "展示「新增时装」面板",
        "type": "bool",
        "default": true
      },
      "module": {
        "description": "展示「新增模组」面板",
        "type": "bool",
        "default": true
      }
    }
  },
  "data": {
    "description": "数据源",
    "type": "object",
    "hint": "数据来自 PRTS Wiki，需要网络。请求条数与「进行中」判定的小时宽限是内部旋钮，不在这里暴露（内核默认 50 条 / 4 小时，可用 GK_API_LIMIT / GK_FUTURE_BUFFER_HOURS 覆盖）。",
    "items": {
      "auto_refresh_daily": {
        "description": "每天首次出图时自动更新数据",
        "type": "bool",
        "default": true,
        "hint": "关闭后只出图不爬取，数据靠自己跑 python cli.py --force 更新。"
      }
    }
  },
  "push": {
    "description": "每日推送",
    "type": "object",
    "hint": "开启后，每天在指定时刻把甘特图推送到下面的目标会话列表。",
    "items": {
      "enabled": {
        "description": "开启每日推送",
        "type": "bool",
        "default": false,
        "hint": "开启前要有推送目标（见下一项）。一份图只渲染一次，然后发给列表里的每个会话。"
      },
      "time": {
        "description": "每日推送时刻（HH:MM，24 小时制）",
        "type": "string",
        "default": "08:00",
        "hint": "按运行 AstrBot 那台机器的本地时间。别设在 00:00~04:00：游戏数据在北京时间 04:00 日切，那段时间出图可能还是前一天的数据。格式不对或越界会退回 08:00。"
      },
      "targets": {
        "description": "推送目标会话列表",
        "type": "list",
        "default": [],
        "hint": "列表为空时，第一个发 /甘特图 的会话会自动被加进来（省去手动填）；之后由你在这里增删——**想停掉某个群的推送，把它从这一项里删掉即可**。会话标识可在目标群发 /甘特图状态 查看。"
      }
    }
  }
}
