# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> AstrBot WebUI 插件页的「更新日志」按钮读的就是本文件（`dashboard/services/plugin_service.py`
> 会在插件目录里找 `CHANGELOG.md`），所以**每个版本都要在这里留一段**。

## [0.1.0] - 2026-09-14

首个版本：把原本只有命令行的绘图脚本做成 AstrBot 插件，同时保留 CLI（`python cli.py`）。

### 新增

- AstrBot 插件入口：`/甘特图`（别名 `/方舟甘特`、`/舟甘特`）、`/甘特图状态`、
  `/甘特图刷新`（管理员）、`/甘特图初始化`（管理员）、`/甘特图帮助`
- 每日定时推送：到点把当天的图推到配置里的目标会话，并附一句「和上次比，新增了什么、时间改了什么、清理了什么」
- 配置项 13 个（`_conf_schema.json`，WebUI 自动生成表单）：标题、时间窗左右范围、提醒天数、
  背景图/背景目录、底栏三个面板开关、每日自动更新、推送开关/时刻/目标列表
- 数据维护策略：初始化时全量回溯一次（`--bootstrap` / `/甘特图初始化`），之后每天只扫
  「进行中 ∪ 近 30 天内结束过」的活动——新增的加进来，过期的删掉
- `tests/`：零依赖契约测试（配置字段、市场元数据、公告年份解析、数据日差、推送记账）
- `LICENSE`：MIT；README 声明游戏素材归鹰角网络、数据归 [PRTS Wiki](https://prts.wiki)、示例背景图不在 MIT 范围内

### 变更

- 数据与代码分家：运行时数据（活动 CSV、当天日差、推送记账）写 `data/plugin_data/<插件名>/`，
  字体与背景图留在插件目录里只读
- 字体不再随包：按「随包 → AstrBot 自定义槽（`data/font.ttf`）→ 平台字体」三层候选，缺字体不再出豆腐块
- 公告里的日期普遍不写年份 → 按所属活动的时间窗推断（此前会推出错误年份，把老活动画到未来）
- 背景图按日期固定取一张，同一天里大家看到的图是同一张
- 推送目标改成配置项 `push.targets`：可以在设置页里增删（想停掉某个群就删掉那一项）；
  列表为空时把第一个用指令的会话自动填进去，之后不再自动添加

### 移除（开发期迭代；这些项只在本仓库的历史里出现过）

- 渲染签名缓存（`插件/缓存.py`）与 `render.reuse_seconds`：每天就出一张图，
  缓存要维护的状态（签名、失效、落盘）比省下的几秒钟贵得多
- `render.random_background`：改用「按日期固定取一张」，换来观感稳定
- 日差里的「不再列出」：抓取是增量的，从「这次没抓到」推不出「源里已经没有」

### 修复

- 剿灭、保全等长期任务检测不出来（日常增量会跳过已结束活动的公告页）
- 公告年份解析错误，导致复刻/长期活动被排到错误的年份

[0.1.0]: https://github.com/Zrief/GanttKnights/releases/tag/v0.1.0
