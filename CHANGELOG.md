# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> AstrBot WebUI 插件页的「更新日志」按钮读的就是本文件（`dashboard/services/plugin_service.py`
> 会在插件目录里找 `CHANGELOG.md`），所以**每个版本都要在这里留一段**。

## [未发布]

### 新增

- `/订阅甘特图`、`/退订甘特图`：显式管理每日推送的目标会话（群聊、私聊都可以，多个会话各订各的）。
- **`notify.require_reminder`（默认关）**：当天没有任何节点提醒时**整次静默**——连图都不发。
  与提醒总开关有组合：总开关关着就永远没有提醒，此时开启本项即永不再推（状态页会警告）。
- **`push.text_template`（默认 `{提醒}`）**：有提醒那天附在图下面的文案可自定义，
  占位符 `{提醒}` / `{日期}` / `{条目数}`；留空 = 只发图。

### 变更

- **设置页跟着开关显示**：没开的功能不再占版面（如没开「开启每日推送」就看不到推送时刻 / 文案模板，
  没开某类提醒就看不到对应的「几天前提醒」）。每日推送时刻改成下拉（要别的时间直接写 `HH:MM` 仍生效）。
- **推送目标改为显式订阅**：原来"`push.targets` 为空时，把第一个发 `/方舟日程` 的会话自动填进去"
  的行为**已移除**——出图不再改配置，没订阅过的会话不会被收进推送。订阅时若配置写不回去
  （旧版 AstrBot 没有 `save_config`），回复里会直接说明，不会让人以为订上了。
- **CLI 产物默认落在 `数据/Gantt.jpg`**（原先是仓库根的 `Gantt.jpg`）：跑一次 `python cli.py`
  不再改脏一个被 git 追踪的文件；仓库根那份现在只是 README 的展示图快照，要更新就手动覆盖。
  换位置仍可用 `GK_OUTPUT_PATH`。
- 内部清理：删掉未使用的 `dataclasses.field` 与无调用者的 `主题.摘要()`，环境变量名收敛为
  `ENV_*` 常量，补齐几处参数注解，`prts_scraper.py` 不在模块级冻结"现在时间"；
  提醒的图标与措辞集中到 `src/提醒文案.py`（改文风只改那里）。

### 修复

- 内核不再抛 `SystemExit`：背景图目录为空时改抛 `背景图缺失`（普通异常），退出码由 CLI 入口翻译
  ——`SystemExit` 不是 `Exception` 子类，会穿透 `asyncio.to_thread` 杀掉宿主的任务链。
- `cli.py` 的 `main()` 返回注解与实现不符（写着 `-> None`，实际 `return 结果`）。

## [0.1.3] - 2026-09-18

### 修复

- **活动时间改为以活动公告为唯一来源，SMW 只当电话簿**（重构 `src/解析_API活动.py`）。
  - 起因：`稳态测定` 的 SMW 时间属性在 wiki 上被抄成了月行水上那批的窗口，图上多出一条
    "明天结束"的幻影——真实窗口 09-28 ~ 10-08，公告里写得明明白白。
  - 新机制：公告解析成板块表后，每个活动按三层认领取时间：① 活动页 `{{活动信息}}` 里
    `公告=` 字段的 #板块锚点（wiki 维护的显式映射，如 此夜同行 → 月行水上公告的签到板块）；
    ② 板块标题含活动名（如「稳态测定签到活动开启」）；③ 都配不上才用 SMW 时间——
    只有肉鸽/生息演算这类没有公告页的常驻条目会走到这。失配单向降级，不再有
    "两个来源各说各话、还都入库"的仲裁。
- 主活动条目改用页面名：显示「逐影集趣」而不是「逐影集趣 「逐影集趣」限时活动开启」。

### 变更

- 日常抓取多读几个活动页本体（拿 `公告=` 指认，约 +5 次请求、一两秒）。
- 清掉五处死代码/旧逻辑：`活动.py` 从未被引用的五个类型常量、`获取公告wikitext` 旧入口、
  字体自检助手 `命中检查`、`initialize()` 里删阶段四 `渲染缓存/` 残留的迁移代码
  （渲染缓存在 0.1.0 发布前已删，市场用户从未有过该目录）、`GK_文本度量` 开关的
  估算式回退分支（真实度量是唯一路径）。

### 移除 / 迁移

- 旧数据里的泛名行（`XX 签到`）与抄错窗口的行不会自动改名——发一次 `/甘特图初始化`
  从零重建即可。

## [0.1.2] - 2026-09-15

### 变更

- **主指令改名为 `/方舟日程`**（别名 `/方舟甘特`）：它是给用户看的"今天有什么日程"，
  而"甘特图"是画法；同时去掉旧别名 `/舟甘特` 与 `/甘特图全量更新`。
  ⚠️ **`/甘特图` 不再出图**（`/甘特图状态`、`/甘特图刷新`、`/甘特图初始化`、`/甘特图帮助` 照旧）。
- **插件展示名改为「明日方舟甘特图」**（原来叫「罗德岛甘特图」）。
- **只在关键节点提醒，不再每天播报**：活动「还剩 N 天结束 / 最后一天 / 开启当天」这类节点命中才发文字，
  平时推送只有一张图。全新配置节 `notify`：
  `enabled`（总开关）/ `end_enabled`、`start_enabled`（两类各自开关）/
  `end_offsets`（默认 `-3, -1`：结束前 3 天与最后一天）/ `start_offsets`（默认 `0`：开启当天）。
  数组就是策略——想提前一周提醒、想在结束当天再提醒一次、想给新活动加个"开启前一天"预告，都只改配置；
  留空数组即那一类不提醒。一次最多列 10 行，超出显示「…另有 N 项」。
- **推送的文字与图片分两条发**（先文字、再图片）：不少平台会把"图 + 文"合成一条带配文的图，
  图片被压小、文字被折叠；分开更像"先说一句、再给图"。
- **推送里不再带"和昨天比变了什么"**（日差仍会算、会落盘、`/甘特图状态` 里能看到日期与
  `/甘特图初始化` 会回显，只是不再每天往群里发）。
- 首页上新（凭证 / 时装 / 模组）**不再出现在提醒文案里**——图里本来就有。
- `/甘特图状态` 与 `/甘特图帮助` 精简（状态页 6 行 → 3 行；帮助页每条指令一行说明）。

### 移除 / 迁移

- `render.remind_days`（过期提醒天数）被 `notify.end_offsets` 取代：老配置里那个键会被忽略，
  新默认行为 = "结束前 3 天 + 最后一天"各提醒一次（与 `remind_days=3` 时的效果不同，见上）。

### 文档

- README 按使用者重写：安装 / 指令 / 每日推送 / **常见问题**（新增）/ **计划中**（新增）/
  配置项 / 许可与**致谢**（新增，列出调研过的 19 个插件仓库及各自的作用）。
  展示图改用绝对 URL —— 相对路径在 AstrBot 插件页会 404。
- `docs/插件化路线.md` 改名 `docs/插件化.md` 并按**执行叙事**重写（519 → 454 行）：只留
  "今天还指导实践"的内容，放弃路线的论证过程收敛成一张表；代码注释里的章节引用从编号改成标题。

## [0.1.1] - 2026-09-15

### 修复

- **缺依赖时不再"能加载但画不出图"**：`main.py` 在导入期就 `import matplotlib`（前面先设好
  `MPLCONFIGDIR`），缺依赖时插件的**导入会失败**，AstrBot 据此自动执行 `requirements.txt`、
  装好再重试导入（走你自己配置的 PyPI 镜像与 AstrBot 的核心依赖约束），用户零操作。
  此前是"插件照常加载、第一次出图才报 `ModuleNotFoundError`"，原因只埋在日志里——
  在新机器上手动拷贝安装时真实踩到过。
- 装不上依赖（例如网络不通）时，插件会在仪表盘显示加载失败并带上原因；
  按官方文档点「尝试一键重载修复」即会先装依赖再加载。

### 内部

- 仓库文档重写为「现状与活知识」（`docs/插件化.md`，1970 → 519 行）：删掉已放弃路线的
  论证过程与阶段 checklist，只留现状、宿主硬事实、踩坑清单与仍然生效的决定

## [0.1.0] - 2026-09-15

首个版本：把原本只有命令行的绘图脚本做成 AstrBot 插件，同时保留 CLI（`python cli.py`）。

### 新增

- AstrBot 插件入口：`/甘特图`（别名 `/方舟甘特`、`/舟甘特`）、`/甘特图状态`、
  `/甘特图刷新`（管理员）、`/甘特图初始化`（管理员）、`/甘特图帮助`
- 每日定时推送：到点把当天的图推到配置里的目标会话，并附一句「和上次比，新增了什么、时间改了什么、清理了什么」
- 配置项 13 个（`_conf_schema.json`，WebUI 自动生成表单）：标题、时间窗左右范围、提醒天数、
  背景图/背景目录、底栏三个面板开关、每日自动更新、推送开关/时刻/目标列表
- 数据维护策略：初始化时全量回溯一次（`--bootstrap` / `/甘特图初始化`），之后每天只扫
  「进行中 ∪ 近 30 天内结束过」的活动——新增的加进来，过期的删掉
- `tests/`：零依赖契约测试（配置字段、市场元数据、公告年份解析、数据日差、推送记账）
- `LICENSE`：MIT；README 声明游戏素材归鹰角网络、数据归 [PRTS Wiki](https://prts.wiki)、示例背景图不在 MIT 范围内

### 变更

- 数据与代码分家：运行时数据（活动 CSV、当天日差、推送记账）写 `data/plugin_data/<插件名>/`，
  字体与背景图留在插件目录里只读
- 字体不再随包：按「随包 → AstrBot 自定义槽（`data/font.ttf`）→ 平台字体」三层候选，缺字体不再出豆腐块
- 公告里的日期普遍不写年份 → 按所属活动的时间窗推断（此前会推出错误年份，把老活动画到未来）
- 背景图按日期固定取一张，同一天里大家看到的图是同一张
- 推送目标改成配置项 `push.targets`：可以在设置页里增删（想停掉某个群就删掉那一项）；
  列表为空时把第一个用指令的会话自动填进去，之后不再自动添加

### 移除（开发期迭代；这些项只在本仓库的历史里出现过）

- 渲染签名缓存（`插件/缓存.py`）与 `render.reuse_seconds`：每天就出一张图，
  缓存要维护的状态（签名、失效、落盘）比省下的几秒钟贵得多
- `render.random_background`：改用「按日期固定取一张」，换来观感稳定
- 日差里的「不再列出」：抓取是增量的，从「这次没抓到」推不出「源里已经没有」

### 修复

- 剿灭、保全等长期任务检测不出来（日常增量会跳过已结束活动的公告页）
- 公告年份解析错误，导致复刻/长期活动被排到错误的年份

[0.1.3]: https://github.com/Zrief/GanttKnights/releases/tag/v0.1.3
[0.1.2]: https://github.com/Zrief/GanttKnights/releases/tag/v0.1.2
[0.1.1]: https://github.com/Zrief/GanttKnights/releases/tag/v0.1.1
[0.1.0]: https://github.com/Zrief/GanttKnights/releases/tag/v0.1.0
