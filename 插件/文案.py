"""用户可见文案 —— 命令回复的单一来源（改文案只改这里）。

> 进度文案只有**一条**（`准备中`）：判断"要不要联网抓数据"是渲染服务的事，
> 入口层为了少发一句话而多做一份预判，只会多出一条会与真判定分叉的路径（阶段四审查实测过）。
> 措辞同时覆盖"数据是今天的（几秒出图）"与"首次出图要抓数据（十几秒）"两种情况。
"""

from __future__ import annotations

准备中 = "博士，正在准备甘特图…"

强制刷新中 = "正在重新抓取 PRTS 数据并重画，请稍候…"

初始化中 = "正在全量重建数据（要逐个扫历史活动的公告，可能需要等十几秒），请稍候…"

渲染失败 = "博士，甘特图生成失败了。相关过程已记入 AstrBot 日志。"

# —— 订阅 / 退订（`/订阅甘特图` / `/退订甘特图`）——
# 与"自动补齐"时代的区别：那时是顺手的便利（写回失败只记日志，用户不必知道）；
# 现在是用户**显式下的命令**，写不进去必须说出来，否则他会以为订阅成功了。

订阅成功 = "好了，这个会话已加入每日甘特图推送。"

订阅已存在 = "这个会话已经在推送目标里了，不用重复订阅。"

退订成功 = "已把这个会话移出每日甘特图推送。"

退订不存在 = "这个会话本来就不在推送目标里。"

推送开关未开 = "提醒：每日推送总开关还关着——到插件设置里打开「开启每日推送」才会真的推送。"

写回失败 = (
    "没能写进配置：{原因}\n"
    "本次运行内仍然有效，但重启后会丢。请到插件设置页的「推送目标会话列表」里{动作}：\n{会话}"
)

缺背景图 = (
    "背景图目录里没有可用图片，无法出图：\n{目录}\n"
    "请把至少一张图片放进该目录后重试（目录可在插件配置里改）。"
)

缺背景文件 = (
    "插件配置里指定的背景图不存在：\n{文件}\n"
    "请检查「指定背景图文件」这一项，或把该文件放进背景图目录里。"
)


def 说明素材缺失(exc) -> str:
    """`素材缺失` 异常 → 给用户看的一句话（区分"目录空"与"指定的文件不在"）"""
    if getattr(exc, "文件", ""):
        return 缺背景文件.format(文件=exc.文件)
    return 缺背景图.format(目录=exc.目录)
