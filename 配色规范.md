# 配色规范（GanttKnights）

**一个种子主色 → 副色 → 警告色。色相先行；饱和度和明度都由规范推导，不查表、不随图片波动。**

参考来源：
- [MaaAssistantArknights](https://github.com/MaaAssistantArknights/MaaAssistantArknights)（`dev-v2` 分支）
  `ColorExtractorHelper.cs` / `MonetPaletteHelper.cs` —— "莫奈取色"的完整实现。
- [astrbot_plugin_palette](https://github.com/Sisyphbaous-DT-Project/astrbot_plugin_palette)
  `palette/colors.py` —— 主色/副色的候选打分与"色相优先"的选取权重。
- Material You / Monet —— "一个源色生成整套角色"的总体思路。

实现见 `src/绘图_主题.py`，由 `main.py` 用随机背景图建主题后交给 `src/绘图_排版.py`，调用方不写死任何色值。

---

## 第一层：色相（从图里取，只回答"这张图是什么颜色"）

**主色**：降采样 32×32 → RGB 每 16 一档分桶 → 跳过纯黑纯白 → 按 **`频率占比 × min(彩度/0.15, 1)²`** 排序。
（MAA 的做法：饱和度取平方，让鲜艳色优先于大面积灰底。注意实现是"彩度增益上限"而非"饱和度加权"。）

**彩度地板 0.05**：排在前面但彩度 < 0.05 的近中性色一律剔除。理由：**近中性色往往是"把画面
各色搅匀后的搅拌色"** —— 实测两张样例图各自排第一的候选，彩度只有 0.031 / 0.032，它们的
"色相"在数学上就是噪声，用噪声定义整套配色的色相会把整张图单调成一种颜色（暖图被单调成土黄）。
全部候选都不够彩时才退让，并按彩度取最大者，避免跳到与图片无关的兜底色相。

**副色**：同样从图里取，但必须是**另一个色相峰** —— 与主色相距 ≥ 40° 的候选里分数最高的那个。
不是同一个峰（实测某样例图副色只差 16.8°，导致强调色与副强调色几乎重合）。
找不到则取主色相 + 35°。

**警告色**：**由主色与副色算出来** —— 两色把色相环切成两段弧，取较长弧的中点，
于是它与主、副色等距且最远，天然是"最跳的那个色"。

## 第二层：饱和度（固定档位，不随图片变化，整体偏莫兰迪）

| 角色 | Oklab 彩度 C | 说明 |
|---|---|---|
| 主色系（强调、色条） | 0.070 | 浊；对应 MAA 的 `PrimarySaturation = 0.36`（HSL）的思路 |
| 大色块（时间条） | 0.042 | 大面积再降一档，往莫兰迪的"浊色"靠 |
| 背景体系（深底、面板、线条） | 0.012 | 只带一点色温 |
| 文字 | 0.006 | 近乎中性 |

> MAA 的关键做法：**饱和度按角色固定**。这样任何一张图都保持同一套观感，
> 不会因为图偏灰就发闷、图偏艳就刺眼。色相才来自图片。

## 第三层：明度（由对比距离推导，不查表）

锚点：深底 0.22（柔和深灰，不用近黑）、更深 0.16、面板 0.95；其余角色的明度由**要拉开多少对比**算出来：

| 角色 | 算法 | 实测对比度 |
|---|---|---|
| 深底正文 | 深底 + 0.74 | 15.4:1 |
| 深底次级 | 深底 + 0.50 | 8.2:1 |
| 深底弱化 | 深底 + 0.34 | 5.2:1 |
| 浅底正文 | 面板 − 0.72 | 14.7:1 |
| 浅底次级 | 面板 − 0.42 | 4.6:1 |
| 强调 / 今天 | 0.72 | 6.8 ~ 8.5:1 |
| 小色块 | 从 0.62 逐档下探直到白字达标 | 4.6:1（守门值 4.5） |

> MAA 的做法：`文字L = 背景L ± TextContrastDistance(0.80)`、`次级 = ±0.56`、`主色 = ±0.40`，
> 深浅模式只是**亮度方向翻转**。明度是"要拉开多少对比"算出来的结果，不是拍脑袋定的档位。

## 分类色：窄色弧 + 明度梯度，两个维度一起做连续过渡

低彩（莫兰迪）下，单靠色相已经分不出类别。所以分类色**同时**沿两个维度走：

```
色相 = 主色相 →（短弧方向，夹在 25°~50°）
明度 = 0.86 → 0.66（由浅到深）
```

事件类型取弧上 4 个等分点、底栏分区取 3 个等分点。这样既保持同族（看起来是一家人），
又能分辨类别，且整体是连续的浊色渐变，不会突然跳到无关色相。

## 面板（底栏三区）配色

三个面板不能只靠"白卡 + 4px 顶部色条"区分：色条本身只有 ΔOkLab ≈ 0.03，缩到手机宽度后
不足 1px，等于没有。**改成一个色分三处落，并把标题行做成色带**：

| 位置 | 取色 | 为什么 |
|---|---|---|
| 标题行色带（占卡片高约 18%，下沿柔化淡出） | 本区色相，alpha 0.95 | 大面积 + 反白字：分区识别度最高，缩放后也不丢 |
| 标题文字 | `对色(色带)` → 白字 | 白字压在饱和色带上最跳，且对比度自动达标 |
| 面板底 | 本区色相，L 0.94、彩度 ×0.022 | 大面积承色，让白卡带一点本区色温 |
| 面板圆角 | 10px | 直角太锐；色带用同一个轮廓裁剪，四角不会冒出去 |

标题带已经承担了"标题行 / 内容"的分隔，所以**不再画带下那条浅色细分隔线** ——
夹在深色带与浅色面板之间会成一条突兀的白线。

穷举过的其他手法（对比图见本地过程稿 `排版草图/面板方案对比.jpg`，不入库）：底渐隐（下色上白）、右下对角、
右渐隐、顶渐隐、均匀色温底。结论：**渐变方向必须避开标题**——顶渐隐会把标题压在饱和色上，
对比度掉下来；底部/右侧渐隐虽然安全，但分区识别度不如"标题色带"直接。

## 字体

**两层两族，四个文件**（`字体/` 不进 git，OFL-1.1，需随文件保留 LICENSE）：

| 层 | 文件 | 用在哪 |
|---|---|---|
| 无衬线 | `NotoSansCJKsc-Regular.otf` / `-Bold.otf` | 正文；页眉标题 30pt、分区名 15pt、干员名、条内事件名用 Bold |
| 等宽 | `NotoSansMono-Regular.ttf` / `-Bold.ttf` | **全部数字与日期**（各 0.5MB） |

- **不用衬线**（2026-09 决定）：衬线原本只用在页眉标题与分区名两处，都是纯中文短串，
  而所有候选字体的汉字 advance 都是 1.000em → 换族对宽度零影响，纯审美选择；旧兜底还会在缺
  `字体/` 时落到系统 `Noto Serif SC`（可变字体 → ExtraLight(200)），标题反而更细。详见
  `docs/插件化路线.md` §10 阶段三·补（逐像素实测：改动只影响这两处字形，版面零位移）。
- **数字必须单独一层**：思源黑的数字 advance 是 0.56em、而斜杠是 **1em 全角**，
  日期会被撑开（实测右上"09/13"明显松散）。等宽层解决对齐，也顺手统一了字重。
- **必须用静态字重文件**：系统那支 `Noto Sans SC` 是**可变字体**，
  matplotlib 没有变体轴接口，只能渲染默认实例 —— 实测默认权重是 Thin(100)，
  所以标题不仅加不粗、反而偏细；缺 Bold 时也只 warning 后降级，**不会伪粗**。
- `src/字体.py` 负责注册四个字重并自检"命中的是仓库文件而不是系统 VF"；
  缺文件时自动退回同色描边伪粗。
- **不做逐字回退混排**（`[Consolas, 思源黑]` 那种是"数字粗、汉字细"的根源）；
  只有中英混排的一整句（如右上副行）才交给 CJK 字体整体渲染。
- 字号：条内事件名 15pt、轴日期 11.5pt、卡片标题 15pt、干员名 13pt、时装名 11pt。

> 来源：[Noto Sans CJK SC](https://github.com/notofonts/noto-cjk/releases/download/Sans2.004/08_NotoSansCJKsc.zip) ·
> [Noto Sans Mono](https://github.com/notofonts/noto-fonts/tree/main/hinted/ttf/NotoSansMono)。
> 注意用带 `CJK` 的语言专用文件，避开系统 VF 的同名冲突。

## 时间轴上的柱带

| 柱带 | 颜色 | 作用 |
|---|---|---|
| 周六 / 周日 | 白，alpha 0.055 | 休息日提示；两侧都是背景图，白色在明暗背景上都成立 |
| **今天** | **本区强调色**，alpha 0.16 | 从"白色柱带"改成强调色，与周末带区分开；仍不画竖线（竖线太抢戏） |
| 刻度 | 每天一个 | 不隔日跳着标；只在真放不下时才跳过（今天永远保留），日期走等宽层 |
| 页眉右侧 | —— | 日期区间在甘特图里已经表达，改成"数据来源 PRTS Wiki + 更新时间"；**不再放时间窗口** —— 加上"窗口 …"后整行 449px、起点落在琥珀竖线左侧 109px，会穿线溢出 |

- **行间距 60 → 36px**：多行时原来行与行之间空得能再塞半张卡，收紧后成组感更强。
- **超大分区会拆行并标注**：如 16 个时装在 14 列网格里拆成 `1/2`、`2/2` 两张卡，
  标题带上带序号（本地过程稿 `排版草图/排版_压力测试_5-16-9.jpg` 是这个极端情况的样例，不入库）。
- **边界情况**（本地过程稿 `排版草图/边界_凌晨4点.jpg`，不入库）：条子放不下名字时用细箭头把名字引到条外，
  3.5 小时这种近乎一条线的短记录也能读到；已结束的事件由筛选规则挡掉，不会进图。

## 透明度

| 元素 | alpha | 说明 |
|---|---|---|
| 背景图 | 0.55 × 按图反解的压暗系数 | 暗纹理 |
| 页眉遮罩 | 横向 0.88→0.10 × 纵向在 0.62 处收到 0 | 只压标题一侧；纵向收尾是为了消掉页眉下沿的硬边（实测原来有 2.6/255 的一步跳变） |
| 面板标题带 | 0.95（下沿指数淡出） | 见上 |
| 日期轴带 | 0.82 | 背景微透 |
| 周末柱带 | 0.055 白 | 见"时间轴上的柱带" |
| 今天柱带 | 0.16 强调色 | 同上 |
| 面板底 | 不透明（带本区色温） | 面板是内容，不跟背景混 |
| 时间条 | 不透明 | 数据要实 |


- **背景图铺满整张画布**（`fig.figimage`，沿用原版 `main.py` 与 MAA 的做法），压暗后垫在
  所有内容之下；轴带用半透明色，背景仍能透出。
- **压暗系数按图反解**，不写死：把合成后的亮度 p95 锁在 `背景目标P95`（现为 0.27）。
  固定系数会把两张性格完全不同的图压成同一种纹理 —— 实测输入反差差 15%、输出极差却完全相同。
  背景图自带的 logo/水印不处理，素材什么样就是什么样。
- **今天用淡色柱带表示**（白色 15% 覆盖整列，沿用原版做法），不画竖线 —— 竖线太抢戏；
  日期轴带上的"今天"刻度用警告色 + 加粗标注即可。
- **事件名写在条上**，不把名称搬到侧栏 —— 眼睛不用在两处来回跳，横向空间也基本留给时间轴；
  条太短放不下时用细箭头引到条外。
- **左侧只留活动类型关键词**（寻访 / 活动 / 福利 / 长期，78px 窄栏）：类型是读图时要判断的
  信息，值得占一列；名称、日期区间、池号、倒计时这些要么能从条上读出、要么与决策无关，不占位。
- **功能块要靠结构区分，不能只靠颜色**：分类色取自同一条窄色弧（相邻类型只差 25° 左右），
  光看颜色分不出块。所以同类型的连续行额外给三重提示：
  ① 左列一条本类型的**色轨**（alpha 0.17，跨满整块）；
  ② 关键词文字用**本类型的条色**，和右侧时间条对上；
  ③ 块与块之间画一条更亮的**分隔线**（主文色 alpha 0.26、1.8px，普通行线只有 1px）。
  这样即使四个类型的颜色相近，边界也是**结构性的**，不依赖辨色。
- **头像保持方形**（只把尖角磨掉一点），照片本身是方的，圆角属于多余的装饰。
- **卡片高度随内容走**：只有干员名的分区（凭证兑换 / 新增模组）不留第二条文字的空位，
  有两行文本的分区（新增时装）自然高一些；同一行内卡片顶对齐。
  模组名与干员名高度重复，`建分区()` 在数据层就不带出来，渲染层不必特判。
- **字号按图片宽度定**：手机上会被缩到屏宽，所以正文不低于图宽的 1.4%
  （条内事件名 15pt ≈ 31px / 2400px）。

## 原则

- **P1 色相先行**：先定主/副/警告三个色相，饱和度和明度都是后续手段。
- **P2 只有三个色**：主色、副色、警告色；副色取主色的邻近色（同族不对冲）。
- **P3 饱和度固定**：按角色定档、整体偏莫兰迪（浊），不随图片波动 —— 这是"规范"的核心。
- **P4 明度由对比距离推导**：先定锚点，其余角色按"要拉开多少对比"算。
- **P5 面积与彩度成反比**：大面积 0.042 浅色，小面积 0.070 中色。
- **P6 对比度守门**：文字与底色 ≥ 4.5:1（WCAG），小色块逐档下探直到白字达标。
- **P7 语义色唯一**：警告色只服务"今天"这一个语义角色，不参与分类。
- **P8 信息只说一遍**：同一条信息不重复出现（名称只在条上，不再另设左侧列重复一次）。
- **P9 不暴露可推导信息**：条目数、池号、倒计时、英文代号、生成时间这类
  要么能从图上直接读出、要么与决策无关的内容一律不放。
- **P10 可读性优先于结构感**：手机上图片会被缩到屏宽，所以字号的下限由"缩到屏宽后
  还能不能看清"决定，而不是由版面留白好不好看决定。

## 实测（两张样例背景图）

| 背景图 | 主 / 副 / 警告 色相 | 4 类时间条（窄弧 + 明度梯度） |
|---|---|---|
| saved_image（暖） | 68° / 154° / 291° | `#e4ccb4` → `#c8ba9e` → `#aca88a` → `#8f9679` |
| 沉沦者（冷） | 233° / 216° / 45° | `#b7d6e8` → `#a0c2d0` → `#89aeb8` → `#749aa0` |
